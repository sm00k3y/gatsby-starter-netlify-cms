import * as React from "react";

const TestComponent = () => {
  return (
    <div>
      <button onClick={() => console.log("Hello")}>Hello</button>
    </div>
  );
};

export default TestComponent;
